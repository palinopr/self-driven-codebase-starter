# Step Decomposition

1. Fill the task plan for scope enforcement before code edits.
2. Add file-scope parsing and changed-file comparison in `src/lib/task-publish.ts`.
3. Add tests for allowed scope and scope drift rejection.
4. Update workflow docs and repo instructions if needed.
5. Run format, docs stamp, validate, and record the results.

## Scope Change Rule

If the implementation requires files outside `src/lib/task-publish.ts`, `test/task-publish.test.ts`, workflow docs, repo instructions, or this task plan folder, record the reason here before widening the diff.
